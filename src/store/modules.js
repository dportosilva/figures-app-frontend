import { store as auth } from '../auth'
import { store as home } from '../pages/home'
import { store as level } from '../pages/level'
import { store as userPage } from '../pages/users'

export default {
    auth,
    home,
    level,
    userPage
}