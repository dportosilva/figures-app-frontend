import { services as auth } from '../auth'
import { services as home } from '../pages/home'
import { services as level } from '../pages/level'
import { services as userPage } from '../pages/users'

export default {
    auth,
    home,
    level,
    userPage
}